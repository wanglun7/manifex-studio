services:
  chroma:
    image: chromadb/chroma:1.5.9
    ports:
      - 8000:8000
    healthcheck:
      test: ['CMD-SHELL', "bash -c 'echo > /dev/tcp/localhost/8000'"]
      interval: 2s
      timeout: 2s
      retries: 15
      start_period: 3s
