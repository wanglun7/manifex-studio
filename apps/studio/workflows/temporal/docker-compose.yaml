name: mastra-temporal-dev

services:
  temporal:
    image: temporalio/auto-setup:latest
    container_name: mastra-temporal-server
    ports:
      - '7233:7233'
      - '8080:8080'
    environment:
      - TEMPORAL_ADDRESS=0.0.0.0:7233
      - TEMPORAL_UI_PORT=8080
